# CHIEF hierarchical causal graph attribution (oracle traversal + progressive filtering) (offline, FakeLLM): differs from the v3 full stack only in representation/attribution algorithms, for atap compare
run_name: offline-chief
seed: 7

source:
  type: jsonl
  path: runs/corpus/traces.jsonl

llm:
  type: fake

stages:
  represent:
    - canonical_events
    - ssf
    - hcg
  attribute:
    - chief
